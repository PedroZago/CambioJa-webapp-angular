import { ValorMaximoDirective } from './valor-maximo.directive';

describe('ValorMaximoDirective', () => {
  it('should create an instance', () => {
    const directive = new ValorMaximoDirective();
    expect(directive).toBeTruthy();
  });
});
